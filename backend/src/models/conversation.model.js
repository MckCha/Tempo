class Conversation {
    constructor({ id, user_id, itinerary_id, session_id }) {
        this.id = id;
        this.user_id = user_id;
        this.itinerary_id = itinerary_id;
        this.session_id = session_id;
    }
}

export default Conversation;
